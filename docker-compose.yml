version: '3.8'
services:
  veritabani:
    image: postgres:15
    container_name: eticaret_pipeline
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=123456
      - POSTGRES_DB=eticaret_db
    ports:
      - "5433:5432"